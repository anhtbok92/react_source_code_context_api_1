import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Red from './Red';

ReactDOM.render(
  <React.StrictMode>
    <Red />
  </React.StrictMode>,
  document.getElementById('root')
);
